# from syn_retro.utils import decompose_mol, dict_to_df

# def test_dict_to_df():
#     # this test should confirm that input dict is converted to a dataframe
#     # and if dict has wrong shape, it should raise an error
#     assert 0

# def test_decompose_mol():
#     # this test should confirm that input mol is decomposed into
#     # fragments and if mol is not decomposable, it should raise an error
#     assert 0
